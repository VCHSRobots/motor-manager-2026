# Database
DATABASE_URL=postgresql://username:password@host:5432/database

# Authentication - Two shared passwords
ADMIN_PASSWORD=lovelove 
USER_PASSWORD=love

# Default admin user (cannot be deleted)
DEFAULT_ADMIN_USERNAME=admin

# Optional: Database connection details (for setup script)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dynamometer_db
DB_USER=postgres