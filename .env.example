# Database
DATABASE_URL=postgresql://username:password@host:5432/database

# Authentication
SHARED_PASSWORD=your_secure_shared_password

# Optional: Database connection details (for setup script)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dynamometer_db
DB_USER=postgres